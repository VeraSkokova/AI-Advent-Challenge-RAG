# 🎄 AI Advent Challenge — Day 16: RAG System

**Проект:** Консольная система RAG (Retrieval Augmented Generation) на Kotlin.  
**Задача:** Реализовать индексацию документов, разбиение на чанки (chunking) и семантический поиск с использованием Yandex Cloud Embeddings API.

---

## 🛠 Технологический стек

Проект реализован на актуальном стеке технологий (согласно требованиям Day 15/Day 10):

*   **Язык:** Kotlin 2.2.20 (JVM)
*   **Сетевой клиент:** Ktor Client 3.0.0 (CIO engine)
*   **Асинхронность:** Kotlin Coroutines 1.9.0
*   **Сериализация:** Kotlinx Serialization 1.6.0
*   **AI API:** Yandex Foundation Models (Embeddings)

## 🚀 Основные возможности

1.  **Умный Чанкинг (Smart Chunking)**
    *   Реализован алгоритм `Sentence-aware Sliding Window`.
    *   Текст разбивается не просто по символам, а по границам предложений.
    *   **Размер окна:** 512 символов.
    *   **Перекрытие (Overlap):** 100 символов (для сохранения контекста на стыках).

2.  **Векторизация (Embeddings)**
    *   Используются модели Яндекса:
        *   `text-search-doc` — для документов.
        *   `text-search-query` — для поисковых запросов.
    *   Реализован **Retry-механизм** (экспоненциальная задержка) для обработки ошибок API (Rate Limits).

3.  **Семантический поиск**
    *   Вычисление косинусного сходства (Cosine Similarity) между вектором запроса и векторами чанков.
    *   Ранжирование результатов по релевантности.

4.  **Персистентность**
    *   Индекс сохраняется на диск в формате JSON (`index.json`).
    *   Поддерживается загрузка существующего индекса при старте.

---

## 📦 Инструкция по запуску

### 1. Предварительная настройка
Создайте файл `local.properties` в корне проекта и добавьте ваши ключи от Yandex Cloud:

```properties
yandex.api.key=AQVN... (ваш API ключ)
yandex.folder.id=b1g... (ваш ID каталога)
```

### 2. Сборка и запуск
Используйте Gradle Wrapper для запуска консольного приложения:

```bash
./gradlew run --console=plain
```

### 3. Пример использования
Поместите ваши `.md` или `.txt` файлы в папку `documents/`. После запуска приложения вы увидите командную строку `>`.

**Индексация документов:**
```bash
> index documents
```
*(Приложение просканирует папку и создаст индекс)*

**Поиск информации:**
```bash
> search "что такое RAG?"
```
*(Выведет наиболее релевантные фрагменты текста)*

**Просмотр статистики:**
```bash
> stats
```

---

## 📊 Пример работы (Лог сессии)

```text
> index documents
🚀 Начинаю индексацию папки: documents
[1/1] Обработка файла: lecture_rag.md
✅ Индексация завершена успешно!

> search зачем нужен overlap?
🔍 Ищу: "зачем нужен overlap?"...

--- Результат #1 (Score: 0.8412) ---
📄 Файл: lecture_rag.md
📝 Текст:
... overlap (перекрытие) необходим для того, чтобы не терять контекст на границах чанков. 
Если важное предложение будет разрезано посередине, нейросеть может не уловить смысл...
```

---

## ✅ Критерии выполнения (Checklist)

- [x] **Индексация:** Приложение сканирует папку и находит `.md` файлы.
- [x] **API Яндекса:** Эмбеддинги успешно генерируются через `text-search-doc`.
- [x] **Chunking:** Реализован алгоритм с перекрытием и учетом предложений.
- [x] **Поиск:** Работает поиск по смыслу (не просто по ключевым словам).
- [x] **Архитектура:** Код разбит на слои (Service, Client, Model).
- [x] **Надежность:** Добавлена обработка ошибок сети и таймаутов.

---

**Автор:** [Ваше Имя / Никнейм]  
**Дата:** 23.12.2025  
*Разработано в рамках AI Advent Challenge* 🎄
